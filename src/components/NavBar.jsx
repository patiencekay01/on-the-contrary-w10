import Menu from "./DropdownMenu"

export default function NavBar () {
    return (
        <nav>
            <div>
            <h1> On The Contrary</h1>
            </div>

            <div>
                <Menu />
            </div>
        </nav>
    )
}